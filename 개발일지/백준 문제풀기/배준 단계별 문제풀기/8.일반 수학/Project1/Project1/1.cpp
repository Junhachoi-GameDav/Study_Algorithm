/*
#include <iostream>
using namespace std;

int main() {
	
	int b=0, n=0, total=0;
	string alphabet;

	cin >> alphabet >> b;
	n = alphabet.length();

	for (int i = 0; i < n; i++)
	{
		if (alphabet[i] >= '0' && alphabet[i] <= '9') {
			total = total * b + (alphabet[i] - 48);
		}
		else {
			total = total * b + (alphabet[i] - 55);
		}
	}

	cout << total;
	return 0;
}
*/
#include <iostream>
using namespace std;

int main() {

}