/*
#include <iostream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);

	int n, find_num_x;
	cin >> n >> find_num_x;
	int* a = new int[n];

	for (int i = 0; i < n; i++)
	{
		cin >> a[i];
		if (a[i] < find_num_x) { cout << a[i] << " "; }
	}
}
*/