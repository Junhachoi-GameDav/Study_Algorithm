auto는 컴파일러가 최적화를 대신 해줘서 std::function 보다 최적화에 좋다. 메모리를 std::는 명시적으로 선언했기
때문에 메모리를 잡아먹지만 auto는 컴파일러가 추론하기때문에 메모리를 컴파일러가 알아서 메모리를 잡을지 말지 결정한다.

대신 auto를 남발하면 못알아볼수있어서 남발하면 안된다. 못 알아볼것 같은 것은 auto를 안쓰는게 좋다.